import styled from "styled-components"

export const Btn = styled.button`
  all: unset;
  line-height: 0;
  display: flex;
  justify-content: center;
  align-items: center;
  user-select: none;
  cursor: pointer;
  line-height: 100%;
  width: fit-content;
  flex-shrink: 0;
`
